import 'package:flutter/material.dart';

class CompletedOrderPageForTechnician extends StatelessWidget {
  const CompletedOrderPageForTechnician({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("صفحة الطلبات المكتملة")));
  }
}
