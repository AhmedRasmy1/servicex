import 'package:flutter/material.dart';

class PendingOrderPageForTechnician extends StatelessWidget {
  const PendingOrderPageForTechnician({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("صفحة الطلبات المعلقة")));
  }
}
